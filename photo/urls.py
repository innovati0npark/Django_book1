from django.urls import path
from . import views

app_name = "photo"

urlpatterns = [
    
    # path('', views..as_view(), name="index"), 
       
    # path("<int:pk>/", views..as_view(), name="detail") #generic에서는 pk로 받는다.
]
